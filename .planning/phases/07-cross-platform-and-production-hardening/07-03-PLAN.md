---
phase: 07-cross-platform-and-production-hardening
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - .github/workflows/ci.yml
  - benchmarks/sequential_write.fio
  - benchmarks/sequential_read.fio
  - benchmarks/random_read.fio
  - benchmarks/small_files.fio
  - benchmarks/run_benchmarks.sh
  - benchmarks/BENCHMARKS.md
autonomous: true
requirements: [PLAT-03, PLAT-04]
must_haves:
  truths:
    - "GitHub Actions CI runs cargo test on Linux with libfuse-dev installed"
    - "GitHub Actions CI runs cargo test on macOS-14 with FUSE-T installed via Homebrew"
    - "CI triggers on push to main and on pull requests"
    - "Windows support (PLAT-03) is documented as deferred to v2 with rationale"
    - "Benchmark scripts exist for sequential write, sequential read, random read, and small file workloads"
    - "Benchmark results are manually reproducible via run_benchmarks.sh"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Linux + macOS CI pipeline"
      contains: "cargo test"
    - path: "benchmarks/run_benchmarks.sh"
      provides: "Benchmark execution script for manual runs"
      contains: "fio"
    - path: "benchmarks/BENCHMARKS.md"
      provides: "Benchmark documentation with targets and methodology"
      contains: "200 MB/s"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "cargo test --workspace"
      via: "GitHub Actions jobs for linux and macos"
      pattern: "cargo test"
    - from: "benchmarks/run_benchmarks.sh"
      to: "benchmarks/*.fio"
      via: "fio job execution"
      pattern: "fio.*\\.fio"
---

<objective>
Set up GitHub Actions CI for Linux and macOS, create benchmark infrastructure, and document Windows deferral.

Purpose: PLAT-04 requires platform-specific testing on both Linux and macOS via CI. PLAT-03 (Windows) is deferred to v2 due to winfsp-rs GPL-3 licensing -- this plan documents that deferral. Benchmarks establish performance baselines for the "daily-driver" claim. CI depends on plan 07-01 (macOS build ergonomics) and 07-02 (stats/scrub commands that CI will test).

Output: CI workflow running on every push/PR; benchmark scripts ready for manual execution; Windows deferral documented.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cross-platform-and-production-hardening/07-CONTEXT.md
@.planning/phases/07-cross-platform-and-production-hardening/07-RESEARCH.md
@.planning/phases/07-cross-platform-and-production-hardening/07-01-SUMMARY.md
@.planning/phases/07-cross-platform-and-production-hardening/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions CI for Linux and macOS + Windows deferral</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create `.github/workflows/ci.yml` with a single workflow containing two jobs:

**Linux job (`linux`):**
- `runs-on: ubuntu-latest`
- Steps:
  1. `actions/checkout@v4`
  2. Install system deps: `sudo apt-get update -q && sudo apt-get install -y libfuse-dev fuse`
  3. `cargo test --workspace` — runs all unit and integration tests
  4. `cargo build -p slicefs-cli` — verifies binary builds

**macOS job (`macos`):**
- `runs-on: macos-14` (pinned — do NOT use `macos-latest` which will migrate to macOS 15 and may break FUSE-T compatibility)
- Steps:
  1. `actions/checkout@v4`
  2. Install FUSE-T: `brew install fuse-t`
  3. `cargo test --workspace` — runs all tests including in-process POSIX compliance tests
  4. `cargo build -p slicefs-cli` — verifies binary builds with FUSE-T linkage

**Trigger:** `on: push: branches: [main]` and `on: pull_request: branches: [main]`

**Windows deferral (PLAT-03):** Add a YAML comment block at the top of the CI file documenting:
```yaml
# Windows CI (PLAT-03): Deferred to v2.
# Reason: winfsp-rs is GPL-3, incompatible with most commercial licensing.
# Alternatives to evaluate in v2: dokan-rs (MIT), Windows ProjFS (native).
# See: .planning/phases/07-cross-platform-and-production-hardening/07-CONTEXT.md
```

This satisfies PLAT-03's requirement to be tracked in this phase while documenting why it is not implemented.

NOTE: Do NOT add pjdfstest to CI yet. The in-process POSIX compliance tests (`cargo test --test posix_compliance_tests`) already run as part of `cargo test --workspace`. Live FUSE mount testing in CI is unreliable (permission issues, /dev/fuse availability). pjdfstest can be added as a follow-up when CI runners are validated.
  </action>
  <verify>
    <automated>cat .github/workflows/ci.yml | head -5 && echo "---" && grep -c "cargo test" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI workflow exists with Linux and macOS jobs; triggers on push to main and PRs; Windows deferral documented as YAML comment; `macos-14` pinned (not `macos-latest`).</done>
</task>

<task type="auto">
  <name>Task 2: Benchmark infrastructure with fio job files and run script</name>
  <files>benchmarks/sequential_write.fio, benchmarks/sequential_read.fio, benchmarks/random_read.fio, benchmarks/small_files.fio, benchmarks/run_benchmarks.sh, benchmarks/BENCHMARKS.md</files>
  <action>
Create the `benchmarks/` directory with fio job files and a runner script.

**fio job files** (4 files):

1. `sequential_write.fio` — sequential write with 4K blocks, 4 jobs, 4GB total, `ioengine=sync`, `direct=1`, `end_fsync=1`. The `directory` parameter is overridden at runtime by the script. Target: 200 MB/s on NVMe.

2. `sequential_read.fio` — sequential read, same parameters but `rw=read`. Reads files created by a prior write pass.

3. `random_read.fio` — random read, `rw=randread`, `bs=4k`, `size=1g`, `numjobs=4`.

4. `small_files.fio` — small file create/delete workload: `rw=write`, `bs=4k`, `size=4k`, `nrfiles=1000`, `numjobs=1`. Tests metadata-heavy operations.

All job files use `[global]` section with `group_reporting=1` and a `directory=${MOUNT_DIR}` placeholder (fio supports `--directory` override on CLI).

**run_benchmarks.sh:**
```bash
#!/usr/bin/env bash
set -euo pipefail

MOUNT_DIR="${1:?Usage: $0 <mount-dir> [results-dir]}"
RESULTS_DIR="${2:-./results}"

mkdir -p "$RESULTS_DIR"
DATE=$(date +%Y%m%d-%H%M%S)

for job in benchmarks/*.fio; do
    name=$(basename "$job" .fio)
    echo "Running: $name"
    fio "$job" --directory="$MOUNT_DIR" \
        --output-format=json \
        --output="$RESULTS_DIR/${name}_${DATE}.json"
    echo "  -> $RESULTS_DIR/${name}_${DATE}.json"
done

echo "All benchmarks complete. Results in $RESULTS_DIR/"
```
Make executable: `chmod +x benchmarks/run_benchmarks.sh`.

**BENCHMARKS.md:**
Document:
- Purpose: performance baselines for SliceFS as a daily-driver filesystem
- Targets: 200 MB/s sequential write on NVMe (target, not hard gate)
- Methodology: fio with direct I/O, fsync-at-end, group reporting
- How to run: `slicefs mount /tmp/mnt --store /tmp/bench-store && ./benchmarks/run_benchmarks.sh /tmp/mnt`
- Job descriptions: one paragraph per .fio file
- Dedup index memory: document that the bloom filter is fixed-size from Phase 1 (bounded memory). Document how to verify: check RSS of slicefs process under load.
- Results section: placeholder "[Run benchmarks and paste results here]"
- Note: Not in CI (hardware-dependent). Developer runs on target hardware.
  </action>
  <verify>
    <automated>ls benchmarks/*.fio benchmarks/run_benchmarks.sh benchmarks/BENCHMARKS.md 2>&1 && head -3 benchmarks/sequential_write.fio</automated>
  </verify>
  <done>Four fio job files exist in benchmarks/; run_benchmarks.sh is executable and runs all jobs; BENCHMARKS.md documents methodology, targets, and has a placeholder for results.</done>
</task>

</tasks>

<verification>
- `.github/workflows/ci.yml` is valid YAML with linux and macos jobs
- `benchmarks/` contains 4 .fio files, run script, and documentation
- Windows deferral is documented in CI workflow comments
- `cargo test --workspace` is the CI test command (covers all in-process tests)
</verification>

<success_criteria>
- CI workflow will run `cargo test --workspace` on both Linux (ubuntu-latest) and macOS (macos-14) for every push to main and PR
- Benchmark infrastructure is ready for manual performance testing
- PLAT-03 (Windows) is documented as deferred to v2 with rationale
- PLAT-04 (platform testing) is covered by CI on both platforms
</success_criteria>

<output>
After completion, create `.planning/phases/07-cross-platform-and-production-hardening/07-03-SUMMARY.md`
</output>
