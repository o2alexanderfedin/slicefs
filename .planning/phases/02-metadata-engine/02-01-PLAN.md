---
phase: 02-metadata-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitmodules
  - crates/data-id
  - crates/slicefs-traits/Cargo.toml
  - crates/slicefs-traits/src/lib.rs
  - crates/slicefs-traits/src/digest.rs
  - crates/slicefs-traits/src/storage.rs
  - crates/slicefs-traits/src/metadata.rs
  - crates/slicefs-traits/src/error.rs
  - crates/slicefs-traits/src/hash.rs
  - crates/slicefs-traits/src/block_store.rs
  - crates/metadata/Cargo.toml
  - crates/metadata/src/lib.rs
  - crates/metadata/src/inode.rs
  - Cargo.toml
autonomous: true
requirements:
  - META-03
  - POSIX-06
  - POSIX-07

must_haves:
  truths:
    - "data-id blockset crate is available as a path dependency from crates/data-id/blockset"
    - "slicefs-traits re-exports Digest224, Digest256, Branches, StorageAdd, StorageGet from blockset"
    - "MetadataStore trait is defined in slicefs-traits with inode CRUD method signatures"
    - "InodeMeta struct serializes/deserializes to exactly 56 bytes (ino, mode, uid, gid, nlinks, size, mtime, ctime)"
    - "cargo test --workspace passes with all existing tests and new InodeMeta round-trip tests"
  artifacts:
    - path: "crates/data-id/blockset/Cargo.toml"
      provides: "data-id blockset crate available as git submodule"
    - path: "crates/slicefs-traits/src/digest.rs"
      provides: "Re-exports of Digest224, Digest256, Branches from blockset"
      exports: ["Digest224", "Digest256", "Branches"]
    - path: "crates/slicefs-traits/src/storage.rs"
      provides: "Re-exports of StorageAdd, StorageGet from blockset"
      exports: ["StorageAdd", "StorageGet"]
    - path: "crates/slicefs-traits/src/metadata.rs"
      provides: "MetadataStore trait, InodeId type alias, DirEntry, MetaError"
      exports: ["MetadataStore", "InodeId", "DirEntry", "MetaError"]
    - path: "crates/metadata/src/inode.rs"
      provides: "InodeMeta binary serialization (56 bytes LE)"
      exports: ["InodeMeta"]
  key_links:
    - from: "crates/slicefs-traits/src/digest.rs"
      to: "blockset::digest224, blockset::digest256, blockset::storage"
      via: "pub use re-exports"
      pattern: "pub use blockset"
    - from: "crates/metadata/Cargo.toml"
      to: "crates/data-id/blockset"
      via: "path dependency"
      pattern: 'blockset.*path'
---

<objective>
Add data-id as a git submodule, redesign slicefs-traits to use Digest224/Digest256 natively, define the MetadataStore trait, and create the metadata crate skeleton with InodeMeta binary serialization.

Purpose: Establish the type foundation that all subsequent metadata plans build on. The data-id integration is the single most important dependency -- nothing else can proceed without it.
Output: data-id submodule wired, slicefs-traits redesigned with blockset types, metadata crate with InodeMeta pack/unpack and round-trip tests.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-metadata-engine/02-CONTEXT.md
@.planning/phases/02-metadata-engine/02-RESEARCH.md

@crates/slicefs-traits/src/lib.rs
@crates/slicefs-traits/src/hash.rs
@crates/slicefs-traits/src/block_store.rs
@crates/slicefs-traits/src/error.rs
@Cargo.toml

<interfaces>
<!-- data-id blockset types (confirmed from source in RESEARCH.md) -->

From blockset (NOT re-exported from lib.rs -- import from module paths):
```rust
// blockset::digest224
pub type Digest224 = [u32; 7];
pub fn to_digest224(d: &Digest256) -> Option<Digest224>;
pub fn from_digest224(k: &Digest224) -> Digest256;

// blockset::digest256
pub type Digest256 = [u32; 8];
pub fn from_bytes(data: &[u8]) -> Option<Digest256>;
pub fn to_data(d: &Digest256) -> Vec<u8>;

// blockset::storage
pub trait StorageAdd { fn add(&mut self, left: &Digest256, right: &Digest256) -> Digest256; fn end(&mut self, x: &Digest256) -> Digest224; }
pub trait StorageGet { fn get(&self, key: &Digest224) -> Option<Branches>; }
pub type Branches = [Digest256; 2];

// blockset::dictionary
pub type Dictionary = BTreeMap<Digest224, Branches>;
pub fn serialize(dict: &Dictionary, write: impl Write);
pub fn deserialize(read: impl Read) -> Dictionary;

// blockset (lib.rs re-exports)
pub use content_dependant_tree::State;
pub use tree::Tree;  // trait with push_all, end
pub use get_data::{GetBytes, GetData};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-id submodule, wire blockset, redesign slicefs-traits</name>
  <files>.gitmodules, crates/data-id, Cargo.toml, crates/slicefs-traits/Cargo.toml, crates/slicefs-traits/src/lib.rs, crates/slicefs-traits/src/digest.rs, crates/slicefs-traits/src/storage.rs, crates/slicefs-traits/src/metadata.rs, crates/slicefs-traits/src/error.rs, crates/slicefs-traits/src/hash.rs, crates/slicefs-traits/src/block_store.rs</files>
  <action>
  1. Clone data-id as git submodule:
     ```
     git submodule add https://github.com/o2alexanderfedin/data-id.git crates/data-id
     ```
     Do NOT add `crates/data-id/blockset` to workspace members (external code, incompatible resolver settings possible). It is only used as a path dependency.

  2. Update workspace Cargo.toml:
     - Add `"crates/metadata"` to `[workspace] members`
     - Do NOT add `crates/data-id/blockset` to members

  3. Update `crates/slicefs-traits/Cargo.toml`:
     - Add `blockset = { path = "../data-id/blockset" }` to `[dependencies]`

  4. Create `crates/slicefs-traits/src/digest.rs`:
     - Re-export Digest224 from `blockset::digest224::Digest224`
     - Re-export Digest256 from `blockset::digest256::Digest256`
     - Re-export Branches from `blockset::storage::Branches`
     - Re-export helper functions: `to_digest224`, `from_digest224`, `from_bytes`, `to_data`
     - NOTE: These are NOT re-exported from blockset's lib.rs -- use full module paths

  5. Create `crates/slicefs-traits/src/storage.rs`:
     - Re-export `StorageAdd` from `blockset::storage::StorageAdd`
     - Re-export `StorageGet` from `blockset::storage::StorageGet`
     - NOTE: These are NOT re-exported from blockset's lib.rs -- use full module paths

  6. Create `crates/slicefs-traits/src/metadata.rs`:
     - Define `pub type InodeId = u64;`
     - Define `MetaError` enum with variants: `NotFound(InodeId)`, `AlreadyExists(InodeId)`, `NotADirectory(InodeId)`, `IsADirectory(InodeId)`, `NotEmpty(InodeId)`, `InvalidName(String)`, `Corrupted(String)`, `Io(#[from] std::io::Error)`
     - Define `DirEntry` struct: `{ pub name: String, pub ino: InodeId }`
     - Define `MetadataStore` trait (Send + Sync, &self methods):
       - `fn create_inode(&self, meta: &InodeMeta) -> Result<InodeId, MetaError>` -- assigns inode number, stores in map+dict
       - `fn get_inode(&self, ino: InodeId) -> Result<InodeMeta, MetaError>`
       - `fn update_inode(&self, meta: &InodeMeta) -> Result<(), MetaError>` -- updates existing inode (same ino)
       - `fn delete_inode(&self, ino: InodeId) -> Result<(), MetaError>`
       - `fn create_directory(&self, parent_ino: InodeId, name: &str, meta: &InodeMeta) -> Result<InodeId, MetaError>` -- creates dir inode + entries with . and ..
       - `fn list_directory(&self, ino: InodeId) -> Result<Vec<DirEntry>, MetaError>`
       - `fn lookup(&self, parent_ino: InodeId, name: &str) -> Result<InodeId, MetaError>` -- lookup by name in directory
       - `fn link(&self, parent_ino: InodeId, name: &str, ino: InodeId) -> Result<(), MetaError>` -- add entry to directory
       - `fn unlink(&self, parent_ino: InodeId, name: &str) -> Result<(), MetaError>` -- remove entry from directory
       - `fn set_manifest(&self, ino: InodeId, blocks: &[Digest224]) -> Result<(), MetaError>`
       - `fn get_manifest(&self, ino: InodeId) -> Result<Vec<Digest224>, MetaError>`
       - `fn set_xattr(&self, ino: InodeId, name: &str, value: &[u8]) -> Result<(), MetaError>`
       - `fn get_xattr(&self, ino: InodeId, name: &str) -> Result<Vec<u8>, MetaError>`
       - `fn list_xattrs(&self, ino: InodeId) -> Result<Vec<String>, MetaError>`
       - `fn remove_xattr(&self, ino: InodeId, name: &str) -> Result<(), MetaError>`
       - `fn root_ino(&self) -> InodeId` -- returns inode 1 (FUSE root)
       - `fn commit(&self) -> Result<Digest224, MetaError>` -- persist current state, return root digest
     - Import `InodeMeta` from metadata crate? No -- InodeMeta is defined in the metadata crate. For the trait, use a forward reference: the trait uses `InodeMeta` which will be defined in `metadata` crate. Since Rust traits can use types from other crates, define InodeMeta fields directly in slicefs-traits/metadata.rs as a plain struct (not the serialization -- just the fields). The metadata crate will use this struct.
     - Actually: define InodeMeta in slicefs-traits itself (it is a data type, not an implementation). This keeps the trait crate self-contained. Include: ino: u64, mode: u32, uid: u32, gid: u32, nlinks: u32, size: u64, mtime_sec: i64, mtime_nsec: u32, ctime_sec: i64, ctime_nsec: u32.
     - Add `InodeMeta::new_directory(ino: InodeId, uid: u32, gid: u32, mode: u32) -> Self` and `InodeMeta::new_file(ino: InodeId, uid: u32, gid: u32, mode: u32) -> Self` convenience constructors that set appropriate defaults (nlinks=2 for dirs, nlinks=1 for files, size=0, timestamps to 0).

  7. Update `crates/slicefs-traits/src/error.rs`:
     - Keep existing CasError as-is (Phase 1 stubs still use it)
     - MetaError is separate in metadata.rs (not merged -- different domains)

  8. Update `crates/slicefs-traits/src/lib.rs`:
     - Add `pub mod digest;` and `pub mod storage;` and `pub mod metadata;`
     - Add re-exports: `pub use digest::*;`, `pub use storage::*;`, `pub use metadata::*;`
     - Keep all existing module declarations and re-exports (hash, chunk, block_store, dedup_index, error)

  9. Update existing `hash.rs` and `block_store.rs`:
     - Keep ChunkHash and existing traits as-is for now (Phase 1 stubs still compile)
     - The old types coexist with the new Digest224-based types -- no breaking change yet
     - cas-local stubs still compile against the old types

  IMPORTANT: Do NOT break existing `cargo test --workspace`. The old ChunkHash-based traits remain for backward compat. New Digest-based metadata trait is additive.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/file-systems && git submodule status crates/data-id && cargo check -p slicefs-traits 2>&1 | tail -5 && cargo check --workspace 2>&1 | tail -5</automated>
  </verify>
  <done>data-id submodule cloned at crates/data-id, blockset wired as path dep in slicefs-traits, digest.rs/storage.rs/metadata.rs created with Digest224/256/MetadataStore/InodeMeta types, all existing tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create metadata crate skeleton with InodeMeta serialization</name>
  <files>crates/metadata/Cargo.toml, crates/metadata/src/lib.rs, crates/metadata/src/inode.rs</files>
  <behavior>
    - InodeMeta round-trip: to_bytes() then from_bytes() recovers all fields exactly
    - InodeMeta to_bytes() produces exactly 56 bytes
    - InodeMeta from_bytes() rejects slices != 56 bytes
    - InodeMeta fields with maximum values (u32::MAX, i64::MIN, etc.) round-trip correctly
    - InodeMeta with ino=0 round-trips (edge case)
    - new_directory() sets nlinks=2, new_file() sets nlinks=1
  </behavior>
  <action>
  1. Create `crates/metadata/Cargo.toml`:
     ```toml
     [package]
     name = "metadata"
     version = "0.1.0"
     edition = "2021"

     [dependencies]
     slicefs-traits = { path = "../slicefs-traits" }
     blockset = { path = "../data-id/blockset" }
     thiserror = { workspace = true }

     [dev-dependencies]
     proptest = { workspace = true }
     ```

  2. Create `crates/metadata/src/lib.rs`:
     - `pub mod inode;` (more modules added in Plan 02 and 03)
     - Doc comment: "Metadata engine for SliceFS -- inode table, directory tree, file manifests, xattrs built on data-id's CAS Dictionary"

  3. Create `crates/metadata/src/inode.rs`:
     - Import `InodeMeta` from `slicefs_traits::metadata::InodeMeta`
     - Implement `to_bytes(&self) -> [u8; 56]` and `from_bytes(buf: &[u8]) -> Result<InodeMeta, MetaError>` as standalone functions (or impl methods on InodeMeta via extension trait, or as free functions in this module)
     - Since InodeMeta is defined in slicefs-traits, implement serialization as free functions: `pub fn serialize_inode(meta: &InodeMeta) -> [u8; 56]` and `pub fn deserialize_inode(buf: &[u8]) -> Result<InodeMeta, MetaError>`
     - Use little-endian byte order, exact layout from RESEARCH.md Pattern 2
     - Return MetaError::Corrupted if buf.len() != 56
     - Add `pub fn intern_inode(dict: &mut impl StorageAdd, meta: &InodeMeta) -> Digest224` that serializes the inode to bytes then calls `State::push_all(dict, &bytes)` to store in Dictionary
     - Add `pub fn load_inode(dict: &impl StorageGet, key: &Digest224) -> Result<InodeMeta, MetaError>` that retrieves bytes via `GetBytes::new(GetData::new(dict, &from_digest224(key)))` then deserializes
     - Write unit tests covering all behaviors listed above
     - Write proptest for arbitrary InodeMeta round-trip

  Verify with `cargo test -p metadata`.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/file-systems && cargo test -p metadata -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>metadata crate exists with InodeMeta serialize/deserialize (56-byte LE), intern_inode/load_inode for Dictionary storage, all round-trip tests pass including proptest</done>
</task>

</tasks>

<verification>
- `cargo check --workspace` passes (no regressions from slicefs-traits changes)
- `cargo test --workspace` passes (existing Phase 1 tests + new InodeMeta tests)
- `git submodule status crates/data-id` shows the submodule is initialized
- `crates/slicefs-traits/src/digest.rs` re-exports Digest224, Digest256
- `crates/metadata/src/inode.rs` has round-trip tests
</verification>

<success_criteria>
- data-id submodule is initialized and blockset compiles as path dependency
- slicefs-traits exports Digest224, Digest256, StorageAdd, StorageGet, MetadataStore trait, InodeMeta struct
- metadata crate exists with InodeMeta binary serialization and Dictionary integration
- All workspace tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-metadata-engine/02-01-SUMMARY.md`
</output>
