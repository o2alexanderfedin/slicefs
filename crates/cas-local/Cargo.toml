[package]
name = "cas-local"
version = "0.1.0"
edition = "2024"
authors = ["Alexander Fedin <af@o2.services>", "Sergey Shandar"]
description = "Stub/test implementations of SliceFS CAS traits (Blake3 hasher, fixed chunker, in-memory and disk block stores, in-memory dedup index)"

[dependencies]
slicefs-traits = { path = "../slicefs-traits" }
blake3.workspace = true
fastbloom.workspace = true
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
tempfile.workspace = true
