[package]
name = "slicefs-traits"
version = "0.1.0"
edition = "2024"
authors = ["Alexander Fedin <af@o2.services>", "Sergey Shandar"]
license = "AGPL-3.0-or-later"
description = "CAS trait contracts for the SliceFS filesystem (ContentHasher, Chunker, BlockStore, DedupIndex)"

[dependencies]
thiserror.workspace = true
blockset = { path = "../data-id/blockset" }
